kind = "web"
previewPath = "/"
title = "FB Token Manager"
version = "1.0.0"
id = "artifacts/fb-token-manager"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25249

[services.development]
run = "pnpm --filter @workspace/fb-token-manager run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fb-token-manager", "run", "build" ]
publicDir = "artifacts/fb-token-manager/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25249"
BASE_PATH = "/"
